#include <iostream>
#include <unistd.h>
#include <stdlib.h>
#include <fstream>
#include <string>
using namespace std;
int main()
{
/*	string line;
	int judge=0;
	while(judge==0){
	ifstream myfile ("tmp"  );
	if (myfile.is_open())
	{
        	getline (myfile,line);
        	if(line=="1"){
        		judge=0;
        	        cout<<"no file submit"<<'\n';
        	        sleep(1000);
        	        }
        	myfile.close();
	}
	}  */
	//system("gcc   1260975782.79.c  -o /var/www/output/1");
	//execl("/usr/bin/gcc", "/usr/bin/gcc", "-o  ",  1260975782.79.c);
	//char * const p[] = {">> a.out"};
  	execv("/var/www/output/1",NULL);
	return 0;
}
